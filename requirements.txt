# Core ML / training
torch>=2.3.0
transformers>=4.40.0
datasets>=2.19.0
trl>=0.9.0
peft>=0.11.0
accelerate>=0.30.0
bitsandbytes>=0.43.0

# Config
pyyaml>=6.0
hydra-core>=1.3.2

# LLM judge & data generation
openai>=1.0.0

# Experiment tracking
wandb>=0.17.0

# Utilities
numpy>=1.26.0

# Cloud compute (optional — only needed for Modal runner)
modal>=0.62.0

# Testing
pytest>=8.0.0

lightning
torchmetrics
torchviz